1) Банальный вопрос - какие основные типы данных существуют в js? number, string, boolean, object, null, undefined.
2) В чем разница между == и === ? == просто сравнивает, === сравнивает по типу.
3) Cпособы объявления переменной ? var,let,const
4) В чем различия между var и let. Их областью видимости - var имеет глобальную область видимости, let - блочную область видимости.
5) Расскажи про map() filter() и reduce(). все перечисленные функции принимают в себя callback, а также массив, при этом map и filter всегда возвращают новый массив, а reduce возвращает результат выполнения функции. map функция изменяет каким-либо образом каждый элемент массива. filter на основе условия возвращает или не возвращает элементы массива тем самым формируя новый массив. reduce функция передает каждой последующей итерации промежуточный результат, то есть например функцию сложения чисел будет передвавть результат сложения каждой итерации.
6) Что такое стрелочная функция и какие у нее особенности по сравнению с обычной функцией. Стрелочная функция - это анонимная функция, которая принимает фиксированное количество аргументов и работают в контексте своего родителя, то есть не имеют своего собсвтвенного this, имеют специфичный синтаксис, который позволяет писать более краткий код.
7) 
